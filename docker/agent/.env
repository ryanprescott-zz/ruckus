# RUCKUS Agent Docker Environment Configuration
# Copy this file to .env and customize for your deployment

# ================================
# DEPLOYMENT CONFIGURATION
# ================================

# Host paths for volume mounts (customize for your system)
MODELS_HOST_PATH=/path/to/your/models
LOGS_HOST_PATH=./logs
CONFIG_HOST_PATH=./config

# Network configuration
AGENT_PORT=8081

# ================================
# AGENT CONFIGURATION
# ================================

# Server connection (set to your RUCKUS server URL)
ORCHESTRATOR_URL=http://ruckus-server:8080
HEARTBEAT_INTERVAL=30

# Performance settings
MAX_CONCURRENT_JOBS=1
JOB_TIMEOUT=3600

# Monitoring settings
METRICS_INTERVAL=1

# ================================
# DEVELOPMENT SETTINGS
# ================================

# Debug mode (set to true for development)
DEBUG=false

# Logging configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# ================================
# EXAMPLE CONFIGURATIONS
# ================================

# For local development:
# MODELS_HOST_PATH=/home/user/models
# ORCHESTRATOR_URL=http://localhost:8080
# DEBUG=true
# LOG_LEVEL=DEBUG

# For production deployment:
# MODELS_HOST_PATH=/data/models
# ORCHESTRATOR_URL=http://ruckus-server.example.com:8080
# DEBUG=false
# LOG_LEVEL=INFO

# ================================
# SYSTEM REQUIREMENTS NOTES
# ================================
# 
# Before running this container, ensure:
# 1. NVIDIA drivers are installed on the host
# 2. Docker with GPU support (docker-compose version 3.8+)
# 3. nvidia-container-toolkit is installed
# 4. Models are downloaded to MODELS_HOST_PATH
# 5. Host system has sufficient GPU memory for target models
#
# Quick setup test:
# docker run --rm --gpus all nvidia/cuda:12.1-base-ubuntu22.04 nvidia-smi